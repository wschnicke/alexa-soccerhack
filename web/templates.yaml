current_status: "The score for the {{team}} match is {{team}} {{team_score}}, {{opponent}} {{opp_score}} at minute {{ minute }}."

alias_success: "{{alias}} is now an alias for team {{team}}."

alias_replace: "replaced alias {{alias}} for team {{team}}."

alias_same: "{{alias}} already exists as an alias for team {{team}}."

alias_fail: "Failed setting {{alias}} as an alias for {{team}}."

next_match_time: "The next {{ team }} match starts on {{ date }} at {{ time }}."

last_result_win: "{{ team }} won the match against {{ opponent }} with a score of {{ team_score }} to {{ opp_score }}"

last_result_win: "{{ team }} lost the match against {{ opponent }} with a score of {{ team_score }} to {{ opp_score }}"

last_result_win: "{{ team }} drew the match against {{ opponent }} with a score of {{ team_score }} to {{ opp_score }}"
